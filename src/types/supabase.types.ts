// Placeholder until the schema migration runs and this is regenerated via:
//   npx supabase gen types typescript --project-id <ref> > src/types/supabase.types.ts
// Using `any` for Database keeps the Supabase client generically typed
// (still type-safe on the client's own methods) without blocking on schema.
export type Database = any;
